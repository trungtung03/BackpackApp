<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <size
        android:width="@dimen/ic_image_width"
        android:height="@dimen/ic_image_height" />
    <corners android:radius="@dimen/ic_image_radius" />
    <solid android:color="@color/white" />
    <stroke
        android:width="@dimen/ic_image_stroke_width"
        android:color="@color/blue" />

</shape>